# Shaltot Suite — الخطة الأساسية

منصّة أدوات محلية (على الجهاز) تجمع أكتر من أداة تحت صفحة واحدة (Hub).
الأداة الأولى: **المترجم الاحترافي (عربي ⇄ إنجليزي)** مع مراعاة السياق القانوني والعلمي.

---

## 1. النطاق (Scope) — إيه اللي بنبنيه بالظبط

| # | المتطلب | الترجمة التقنية |
|---|---------|-----------------|
| 1 | رفع أي ملف (PDF / Word / Excel / PowerPoint / نص) | طبقة Ingest متعددة الصيغ |
| 2 | استخراج النص كامل بشكل منظّم | Document Model + Anchors (مؤشرات رجوع للملف الأصلي) |
| 3 | ترجمة الملف كامل بجودة عالية | Translation Engine + سياق + مصطلحات |
| 4 | صفحة مراجعة عربي/إنجليزي جنب بعض مقطع بمقطع | Bilingual Review Grid |
| 5 | تعديل في النص العربي أو الإنجليزي | تحرير ثنائي الاتجاه + إعادة توليد |
| 6 | التعديل يتطبّق على كل تكرارات نفس الكلمة/المقطع | Propagation Engine + Translation Memory |
| 7 | تصدير بنفس الصيغة والتنسيق الأصلي بالظبط | Merge-back عبر الـ Anchors (مش إعادة بناء) |
| 8 | حاسبة تكلفة لكل مشروع | Usage Ledger + Cost Calculator |

**مبدأ حاكم:** إحنا مش بنعيد بناء الملف من الصفر، إحنا بناخد **نسخة من الملف الأصلي** وبنستبدل النص جوّاه في مكانه. ده السبب الوحيد اللي يخلّي التنسيق يطلع مطابق ١٠٠٪.

---

## 2. المعمارية العامة

```
┌──────────────────────────────────────────────────────────┐
│  الواجهة (React + Vite)                                   │
│  ┌──────────┐  ┌─────────────┐  ┌──────────┐  ┌────────┐ │
│  │ Hub      │  │ المترجم      │  │ المراجعة  │  │ التكلفة │ │
│  └──────────┘  └─────────────┘  └──────────┘  └────────┘ │
└───────────────────────────┬──────────────────────────────┘
                            │ REST + SSE (تقدّم لحظي)
┌───────────────────────────┴──────────────────────────────┐
│  الخادم (FastAPI / Python 3.12)                           │
│  ┌────────┐ ┌─────────┐ ┌──────────┐ ┌────┐ ┌──────────┐ │
│  │ Ingest │→│ Extract │→│ Segment  │→│ TM │→│ Translate│ │
│  └────────┘ └─────────┘ └──────────┘ └────┘ └────┬─────┘ │
│                                                   ↓       │
│  ┌────────┐ ┌─────────┐ ┌──────────┐ ┌───────────────┐   │
│  │ Export │←│ Merge   │←│ Review   │←│ QA Checks     │   │
│  └────────┘ └─────────┘ └──────────┘ └───────────────┘   │
└───────────────────────────┬──────────────────────────────┘
                            │
        ┌───────────────────┴────────────────────┐
        │ SQLite (SQLAlchemy) + ملفات على القرص   │
        │ Projects · Segments · TM · Glossary ·   │
        │ UsageLedger · Jobs                      │
        └────────────────────────────────────────┘
```

**الستاك المقترح:**
- **Backend:** Python 3.12 + FastAPI — لأن كل مكتبات معالجة المستندات المحترمة بايثون (`python-docx`, `openpyxl`, `python-pptx`, `PyMuPDF`, `lxml`).
- **Frontend:** React + TypeScript + Vite + Tailwind — واجهة سريعة، وشبكة المراجعة محتاجة Virtualized Table.
- **DB:** SQLite محليًا (نفس الكود يشتغل على Postgres بعدين من غير تغيير).
- **مهام خلفية:** Worker داخلي بطابور + SSE للتقدّم اللحظي (من غير Redis/Celery في البداية).
- **التشغيل:** أمر واحد `start.bat` يشغّل الخادم ويفتح المتصفح.

---

## 3. الـ Hub (صفحة تجميع الأدوات)

مش صفحة ثابتة — **سجلّ أدوات (Tool Registry)**: كل أداة عبارة عن Plugin ليه ملف تعريف (اسم، أيقونة، وصف، مسار API، مسار واجهة). إضافة أداة جديدة بعدين = ملف تعريف + مجلد، من غير ما نلمس الأساس.

```
tools/
  translator/          ← الأداة الأولى
    manifest.json
    backend/
    frontend/
  <أداة تانية لاحقًا>/
```

---

## 4. خط سير الترجمة (القلب التقني)

### 4.1 الاستخراج — Extract (لكل صيغة على حدة)

الفكرة المحورية: كل قطعة نص بتترجم لازم يبقى ليها **Anchor** = عنوان دقيق يرجّعنا لمكانها في الملف الأصلي.

| الصيغة | المكتبة | نغطّي إيه | الـ Anchor |
|--------|---------|-----------|------------|
| **DOCX** | `python-docx` + `lxml` مباشرة على OOXML | الفقرات، الجداول (والمتداخلة)، الرؤوس والتذييلات، الحواشي، مربعات النص `w:txbxContent`، التعليقات، العناوين، القوائم المرقّمة، الروابط | `(part, paragraph_id, run_range)` |
| **XLSX** | `openpyxl` | الخلايا النصية، أسماء الأوراق، التعليقات، رؤوس/تذييلات الطباعة، عناوين الرسوم، قوائم التحقق | `(sheet, cell)` |
| **PPTX** | `python-pptx` | كل شكل فيه نص، المجموعات، الجداول، الرسوم، **صفحات الملاحظات**، القوالب | `(slide, shape_id, para, run_range)` |
| **PDF** | `PyMuPDF` | مسارين — انظر قسم القرارات | حسب المسار |
| **TXT/MD/CSV/HTML** | مباشر | كامل | `(line/node)` |

**تحذيرات مهمة اتحطّت في الحسبان:**
- في XLSX: **مانترجمش المعادلات** — بنترجم النصوص الحرفية والنتائج المخزّنة بس، والأرقام والتواريخ تفضل زي ما هي.
- في DOCX: الجملة الواحدة ممكن تكون متقسّمة على 5 `runs` بسبب التنسيق. بنعمل **دمج ذكي للـ runs** قبل الترجمة، وبنحوّل التنسيق الداخلي لـ Placeholders: `النص {1}المهم{/1} هنا` — وبنرجّعه بعد الترجمة. من غير الخطوة دي التنسيق بيضيع.
- في PPTX: النص الإنجليزي أطول من العربي بحوالي **15–25%** — بنطلع تقرير "أماكن محتمل تطفح" مع اقتراح تصغير الخط.

### 4.2 التقسيم — Segment
مقسّم جُمل واعي بالعربي: يتعامل مع `؟ ! . :` والاختصارات والأرقام والترقيم القانوني (`المادة (5)`، `البند 3/ب`) من غير ما يكسرها.
كل مقطع بياخد `segment_id` ثابت + hash للمصدر (ده أساس الذاكرة والانتشار).

### 4.3 الترجمة — Translate
لكل دفعة مقاطع (~2000–3000 توكن) بنبعت:
- **System prompt ثابت** (بيتخزّن في الـ Prompt Cache → أرخص 10 مرات): دليل الأسلوب + نوع المجال + المصطلحات المعتمدة.
- **السياق:** المقطع السابق واللاحق (عشان الضمائر والاتساق).
- **مخرجات مقيّدة (Structured Outputs)** بـ JSON Schema: `[{id, target, confidence, notes}]` — يضمن إن كل مقطع يرجع مربوط بالـ id بتاعه من غير أي parsing هش.
- **أوضاع المجال:** `قانوني` / `علمي` / `طبي` / `تقني` / `عام` — كل واحد ليه system prompt متخصص:
  - *قانوني:* حرفية عالية، الحفاظ على الغموض المتعمّد، مصطلحات ثابتة (`الطرف الأول = First Party`)، عدم تفسير، الحفاظ على أرقام المواد والإحالات.
  - *علمي:* دقة المصطلح، الحفاظ على الوحدات والرموز والمعادلات كما هي.

### 4.4 الدمج والتصدير — Merge & Export
بناخد **نسخة بايت-بايت من الملف الأصلي** ونمشي على الـ Anchors نستبدل النص. النتيجة: نفس الصيغة، نفس الخطوط، نفس الجداول، نفس الصور، نفس الترويسة.

**معالجة الاتجاه (RTL → LTR)** — نقطة بتفشل فيها أغلب الأدوات:
- DOCX: إزالة `w:rtl` و `w:bidi`، قلب المحاذاة يمين→يسار، `w:bidiVisual` في الجداول، ضبط `w:lang` على `en-US`.
- XLSX: `sheetView rightToLeft="0"` + محاذاة الخلايا.
- PPTX: `a:pPr rtl="0"` + محاذاة الفقرات.
- الأقواس والأرقام والعلامات: إصلاح ترتيبها بعد قلب الاتجاه.

---

## 5. الذاكرة والمصطلحات والانتشار (المتطلب #6)

ده الجزء اللي بيحوّل الأداة من "مترجم" لـ "نظام ترجمة احترافي".

**أ) ذاكرة الترجمة (Translation Memory):**
كل زوج (مصدر ← هدف) معتمَد بيتخزّن. في أي مشروع جديد: مطابقة 100% تتعبّى تلقائيًا (وببلاش)، ومطابقة 75–99% تتعرض كاقتراح مع إبراز الفروق.

**ب) قاعدة المصطلحات (Termbase / Glossary):**
جدول مصطلحات لكل عميل/مجال (`عقد إذعان → Contract of Adhesion`)، بيتحقن في الـ prompt وبيتفحص بعد الترجمة.

**ج) محرّك الانتشار (Propagation) — قلب المتطلب:**
لما المراجع يعدّل مقطع أو كلمة، بيحصل تلات حاجات:
1. **مطابقة تامة:** كل المقاطع اللي نفس المصدر بالظبط → تتحدّث تلقائيًا (مع إمكانية التراجع).
2. **على مستوى المصطلح:** لو التعديل غيّر ترجمة مصطلح، بنعمل مسح للملف كله ونعرض **قائمة بكل المواضع** مع checkbox لكل موضع + معاينة قبل/بعد → المراجع يوافق على اللي عايزه.
3. **حفظ التعلّم:** التعديل يتسجّل في الذاكرة والمصطلحات، فالمشاريع الجاية تستفيد منه تلقائيًا.

> ليه بموافقة مش تلقائي بالكامل؟ لأن نفس الكلمة ممكن يبقى ليها ترجمة مختلفة حسب السياق. الأدوات الاحترافية (Trados/memoQ) بتشتغل بنفس المنطق ده.

---

## 6. فحوصات الجودة الآلية (QA)

بتشتغل تلقائي وبتظهر كأعلام على المقاطع في شاشة المراجعة:

| الفحص | بيمسك إيه |
|-------|-----------|
| تطابق الأرقام | رقم في المصدر مش موجود في الهدف (كارثي في العقود) |
| سلامة الوسوم | placeholder تنسيق ضايع أو ناقص |
| التزام المصطلحات | مصطلح معتمد اتترجم بشكل مختلف |
| الاتساق الداخلي | نفس المصدر اتترجم بطريقتين في نفس الملف |
| مقاطع فارغة/غير مترجمة | نص عربي فاضل في المخرجات |
| شذوذ الطول | الهدف أطول/أقصر بشكل غير طبيعي (مؤشر هلوسة أو حذف) |
| علامات الترقيم والأقواس | أقواس غير متوازنة |

---

## 7. شاشة المراجعة

- **جدول ثنائي اللغة** (Virtualized): مقطع بمقطع، المصدر يمين (RTL) والهدف يسار (LTR).
- تحرير **الاتجاهين**: تعديل الهدف مباشرة، أو تعديل المصدر → إعادة ترجمة المقطع ده بس.
- **فلاتر:** غير مراجَع / معدّل / عليه أعلام QA / مطابقات الذاكرة / مكرر.
- **اختصارات لوحة المفاتيح:** `Ctrl+Enter` اعتماد وانتقال، `Ctrl+↑/↓` تنقّل، `Ctrl+K` بحث/استبدال، `Ctrl+G` إدراج مصطلح.
- **معاينة سياقية:** عرض المقطع في مكانه من المستند (صفحة/شريحة/خلية).
- **حالة لكل مقطع:** مسوّدة → مراجَع → معتمَد. التصدير بيقول لك كام مقطع لسه غير معتمد.
- بحث واستبدال على مستوى المشروع كله مع معاينة.

---

## 8. حاسبة التكلفة (المتطلب #8)

**قبل التشغيل — تقدير:**
عدد الكلمات، عدد الصفحات الحقيقي، عدد المقاطع، التوكنز المقدّرة (عبر `count_tokens` الرسمي)، والتكلفة المتوقعة لكل موديل → المستخدم يوافق قبل ما يتصرف قرش.

**أثناء وبعد التشغيل — سجلّ فعلي (Usage Ledger):**
كل نداء API بيتسجّل: الموديل، input/output tokens، cache read/write، batch أو لا، الوقت، التكلفة المحسوبة.

**التقارير:**
- تكلفة المشروع الواحد + التكلفة لكل كلمة + التكلفة لكل صفحة.
- **الوفورات:** كام وفّرنا من ذاكرة الترجمة (مقاطع ماترجمتش)، من الـ Prompt Cache، ومن الـ Batch API.
- لوحة شهرية: إجمالي الإنفاق، توزيع حسب الموديل والمشروع.

**جدول الأسعار (يتحدّث من ملف إعدادات — لكل مليون توكن):**

| الموديل | إدخال | إخراج | الاستخدام المقترح |
|---------|-------|-------|-------------------|
| Claude Opus 5 | $5.00 | $25.00 | العقود والمستندات القانونية الحرجة |
| Claude Sonnet 5 | $3.00 (عرض تعريفي **$2.00** حتى 2026-08-31) | $15.00 (عرض **$10.00**) | **الافتراضي** — جودة قريبة من Opus بثلث السعر |
| Claude Haiku 4.5 | $1.00 | $5.00 | مهام مساعدة (فحص، استخراج مصطلحات) |

**مضاعِفات مهمة في الحساب:**
- **Batch API: خصم 50%** على كل شيء (مناسب للملفات الكبيرة اللي مش مستعجلة — بتخلص عادة في أقل من ساعة).
- **Prompt Cache:** القراءة بـ **0.1×** من سعر الإدخال، والكتابة 1.25× — يعني الـ system prompt والمصطلحات بيتدفع تمنها مرة واحدة.

**تقدير واقعي:** مستند 10,000 كلمة عربي ≈ **$0.30 على Sonnet 5** أو **~$0.75 على Opus 5** — يعني حوالي **٣–٨ سنت للصفحة**. مع الـ Batch يتنصّف تاني.

---

## 9. تصميم قابل للتبديل (Engine Adapters)

واجهة موحّدة `TranslationEngine` عشان نبدّل المحرّك من غير ما نمس باقي النظام:

```
engines/
  claude.py       ← الأساسي (Opus 5 / Sonnet 5 / Haiku 4.5)
  deepl.py        ← اختياري
  google.py       ← اختياري
  local_nllb.py   ← نموذج مفتوح المصدر يشتغل أوفلاين (بديل مجاني)
```
كل محرّك بيبلّغ عن تكلفته، فالحاسبة شغالة مع الكل.

---

## 10. الأمان وحماية البيانات

- مفاتيح الـ API في `.env` محلي (مش في الكود ولا في الـ DB)، والملف في `.gitignore`.
- الملفات كلها تتخزّن محليًا على الجهاز `storage/projects/{id}/`.
- سجلّ تدقيق (Audit Log) لكل تعديل: مين، إمتى، قبل وبعد.
- خيار "وضع أوفلاين بالكامل" للمستندات شديدة الحساسية (يستخدم المحرّك المحلي).

---

## 11. هيكل المشروع

```
Shaltot/
├─ backend/
│  ├─ app/
│  │  ├─ main.py            # FastAPI
│  │  ├─ core/              # config, db, security
│  │  ├─ models/            # SQLAlchemy
│  │  ├─ api/               # routes
│  │  ├─ jobs/              # worker + queue + SSE
│  │  └─ tools/
│  │     └─ translator/
│  │        ├─ ingest/      # كشف الصيغة
│  │        ├─ extract/     # docx, xlsx, pptx, pdf
│  │        ├─ segment/     # مقسّم الجُمل العربي
│  │        ├─ engines/     # claude, deepl, local
│  │        ├─ tm/          # الذاكرة + المصطلحات + الانتشار
│  │        ├─ qa/          # الفحوصات
│  │        ├─ merge/       # الإرجاع للملف الأصلي + RTL
│  │        └─ costing/     # الحاسبة
│  └─ tests/
├─ frontend/
│  └─ src/
│     ├─ pages/  (Hub, Project, Review, Costs, Settings)
│     └─ components/
├─ storage/                 # الملفات (خارج git)
├─ .env.example
├─ start.bat
└─ PLAN.md
```

---

## 12. خطة التنفيذ بالمراحل

| المرحلة | المحتوى | المخرج القابل للاستخدام |
|---------|---------|--------------------------|
| **0. الأساس** | المستودع، الإعدادات، قاعدة البيانات، Hub، رفع ملف | تشغّل الأداة وترفع ملف |
| **1. الشريحة الرأسية (DOCX)** | استخراج → تقسيم → ترجمة → مراجعة → تصدير Word | **أول ملف Word مترجم بتنسيقه بالكامل** |
| **2. Excel + PowerPoint** | نفس الخط للصيغتين | تغطية أوفيس كاملة |
| **3. PDF** | حسب القرار في القسم الجاي (+ OCR للممسوح ضوئيًا) | تغطية PDF |
| **4. الذاكرة والمصطلحات والانتشار** | TM + Termbase + محرّك الانتشار + فحوصات QA | اتساق على مستوى المستند والمشاريع |
| **5. حاسبة التكلفة** | التقدير المسبق + السجلّ + التقارير + الوفورات | شفافية تكلفة كاملة |
| **6. التلميع** | الاختصارات، الأداء، التغليف، النسخ الاحتياطي | نسخة 1.0 |

المراحل من 1 لتحت كل واحدة بتطلع **حاجة شغالة تقدر تجرّبها**، مش كود على الرف.

---

## 13. القرارات المعتمَدة ✅

| القرار | الاختيار | الأثر على البناء |
|--------|----------|------------------|
| **ترتيب الصيغ** | الأربعة بالتوازي | طبقة استخراج موحّدة للأربع صيغ تتبني الأول كأساس مشترك، وبعدين يمشي عليها باقي الخط |
| **استراتيجية PDF** | PDF → Word قابل للتعديل | مسار الـ PDF بيتحوّل لـ DOCX بدري ويستخدم **نفس** محرّك الاستخراج والدمج بتاع Word — مش مسار منفصل |
| **المحرّك** | Sonnet 5 افتراضي + Opus 5 للقانوني | مُبدِّل موديل لكل مشروع، والحاسبة بتقارن التكلفة بين الاتنين |
| **المستخدمون** | مستخدم واحد محلي | بدون حسابات ولا صلاحيات — يفضل سجلّ التدقيق للتاريخ فقط |

> ملاحظة على الجهاز: مفيش LibreOffice / Tesseract / Docker مثبّتين. للـ PDF الممسوح ضوئيًا هنستخدم قدرة الرؤية في Claude للـ OCR بدل تثبيت Tesseract.

### حالة التنفيذ

| المرحلة | الحالة | ملاحظات |
|---------|--------|---------|
| **0** الهيكل الأساسي | ✅ | FastAPI + SQLite + التخزين + الرفع |
| **1** طبقة الاستخراج للأربع صيغ | ✅ | Word · Excel · PowerPoint · PDF · نص |
| **1ب** المقسّم العربي | ✅ | 16/16 حالة اختبار |
| **2** محرّك الترجمة | ✅ | Sonnet 5 / Opus 5 + مخرجات مقيّدة + تخزين مؤقت |
| **3** واجهة المراجعة الثنائية | ✅ | تحرير + فلاتر + اختصارات + حوار انتشار |
| **4** الدمج والتصدير + RTL→LTR | ✅ | مختبَر بدورة كاملة لكل صيغة |
| **5** الذاكرة + المصطلحات + الانتشار + QA | ✅ | 7 فحوصات جودة آلية |
| **6** حاسبة التكلفة | ✅ | تقدير مسبق + سجلّ فعلي + الوفورات |
| **7** التلميع والتغليف | ✅ | `start.bat` + توثيق + 5 مجموعات اختبار |

### الجولة الثانية — بعد التشغيل الفعلي

| البند | الحالة | الأثر المقيس |
|-------|--------|--------------|
| إصلاح قفل قاعدة البيانات (الحفظ التدريجي) | ✅ | كان بيوقّف الترجمة على الملفات الكبيرة بخطأ 500 |
| حاجز الترجمة المكررة | ✅ | ضغطتين على «ترجمة» كانوا بيحسبوا مرتين |
| حاجز الذاكرة ضد المخرجات التجريبية | ✅ | كانت بتلوّث كل مشروع حقيقي بعدها |
| فحص الفاصل الرقمي الملتبس | ✅ | `150.000` بتتقري 150 في الإنجليزي |
| تعليمات عربي→إنجليزي + تجاوز حد الكاش | ✅ | التكلفة نزلت 7× في النداءات بعد الأولى |
| التنفيذ المتوازي | ✅ | تسريع 2.7× مقيس · ملف 100 صفحة: 38 → 10 دقايق |
| استرجاع المهام المعلّقة | ✅ | المهام كانت بتفضل «شغالة» للأبد بعد إقفال الخادم |
| القراءة الضوئية (OCR) | ✅ | 10/10 كتلة مقروءة صح · ~سنت للصفحة |
| الدفعات المجمّعة (Batch API) | ✅ | وفر فعلي 50%→4% حسب الحجم (انظر أدناه) |
| تنظيف مجلدات المشاريع المحذوفة | ✅ | أداة صيانة |

**متبقٍ عن قصد:**
- ترجمة أسماء أوراق Excel — بتكسر المعادلات اللي بتشير للورقة.
- الملفات الممسوحة بجودة رديئة جدًا — النموذج بيكتب `[…]` مكان الكلمة
  غير المقروءة بدل ما يخمّن، والمراجع بيقرر.

---

*آخر تحديث: 2026-08-12*
